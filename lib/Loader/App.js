var modules = {
    express: require('express'),
    config_parser: require('../Config/Parser'),
    http: require('http'),
    finalhandler: require('finalhandler')
};

var configs = {
};

var services = {
};

module.exports = {
    modules: modules,
    configs: configs,
    services: services
};

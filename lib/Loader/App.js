var modules = {
    express: require('express'),
    config_parser: require('../Config/Parser')
};

var configs = {
};

var services = {
};

module.exports = {
    modules: modules,
    configs: configs,
    services: services
};

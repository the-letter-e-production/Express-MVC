var modules = {
    moment: require('moment'),
    winston: require('winston'),
    express_winston: require('express-winston')
};

var configs = {
};

var services = {

};

var objects = {
};

var utils = {
};

module.exports = {
    modules: modules,
    configs: configs,
    services: services,
    objects: objects,
    utils: utils
};
